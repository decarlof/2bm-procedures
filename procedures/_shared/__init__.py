"""Cross-procedure primitives.

Modules here are intentionally small and dependency-light. If a helper
grows beyond a single concern, split it into a sibling module rather
than letting one file accumulate unrelated utilities.
"""
